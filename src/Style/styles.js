import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  baseText: {
    fontSize: 25,
    fontFamily: "Cochin",

  },
  titleText: {
    fontSize: 30,
    fontWeight: "bold"
  }
});